# Movie List

This is a basic Rails 4 application that serves up a list of movies and
allows CRUD functionality.

Before use, please run
`rake db:seed`
in the console in order to seed the data into the database.
